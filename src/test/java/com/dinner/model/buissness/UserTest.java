/*
package com.dinner.model.buissness;

import com.dinner.model.domain.user.Account;
import com.dinner.model.domain.user.User;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

*/
/**
 * Created by Tomek on 19-Feb-17.
 *//*

public class UserTest {
    private User user;

    @Before
    public void init() {
        user = new User();
        user.setFirstName("Uset");
        user.setLastName("Test");
        user.setEmail("u@t.com");
        user.setPassword("aaa");
        user.setAccount(new Account(20.00));
    }

    @Test
    public void withdrawAndReturnMoney() {
        user.getAccount().withdraw(15.00);
        user.getAccount().returnCash(15.00);
        Assert.assertEquals(user.getAccount().getMoney(), 20.00,0.001);
    }
}
*/
